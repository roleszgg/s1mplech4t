# Copy this file to .env and set a real secret before running in anything
# beyond local testing. JWT_SECRET signs the login tokens — if it leaks,
# anyone can forge a valid session for any user.
JWT_SECRET=replace-with-a-long-random-string
PORT=3000
